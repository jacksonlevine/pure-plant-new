---
import { defaultLocale } from '../../lib/i18n/config';
import { localizedPath } from '../../lib/i18n/paths';

const { slug } = Astro.params;

Astro.redirect(localizedPath(defaultLocale, `/products/${slug}`));
---